export type ScanRecord = {
  id: number;
  seed: number;
  x: number;
  y: number;
  timestamp: Date;
  note: string;
};

export type PastSpectrumRecord = {
  id: number;
  seed: number;
  x: number;
  y: number;
  timestamp: Date;
  note: string;
};
